1C:Enterprise 8.3 Remote Administrative Client Utility ? 1C-Soft LLC 1996-2017
Утилита администрирования платформы 1С:Предприятие

Использование:

	rac help [options] [arguments]

Общие параметры:

    --version | -v
        получение версии утилиты

    --help | -? | -h
        отображение краткой информации об утилите

Общие аргументы:

    <host>[:<port>]
        адрес сервера администрирования (по-умолчанию: localhost:1545)

Режим:

    help (h)
        Отображение справочной информации для указанного режима.

Аргументы:
    MODE
        режим, для которого необходимо получить информацию о ключах командной строки

Поддерживаемые режимы:

help          Отображение справочной информации для указанного режима.
agent         Режим администрирования агента кластера серверов
cluster       Режим администрирования кластера серверов
manager       Режим администрирования менеджера кластера серверов
	Функция МенеджерПолучитьОписание(Кластер, Менеджер, АдминистраторКластера)
	Функция МенеджерПолучитьСписок(Кластер, АдминистраторКластера)
	
server        Режим администрирования рабочего сервера
	Процедура СерверДобавить(Кластер, АдресАгента, ПортАгента, Имя, ДиапазонПортов, ВариантИспользования, МаксКоличествоБаз, МаксОбъемПамяти, МаксКоличествоСоединений, ПортМенеджера, РазмещениеМенеджеров, ОграничениеПамятиПроцессов, РасходПамятиНаВызов, АдминистраторКластера)
	Процедура СерверИзменить(Кластер, Сервер, ДиапазонПортов, ВариантИспользования, МаксКоличествоБаз, МаксОбъемПамяти, МаксКоличествоСоединений, РазмещениеМенеджеров, ОграничениеПамятиПроцессов, РасходПамятиНаВызов, АдминистраторКластера)
	Процедура СерверУдалить(Кластер, Сервер)
	
process       Режим администрирования рабочего процесса
	Функция ПроцессПолучитьОписание(Кластер, Процесс, ПоказыватьЛицензии, АдминистраторКластера)
	Функция ПроцессПолучитьСписок(Кластер, ПоказыватьЛицензии, АдминистраторКластера)

service       Режим администрирования сервиса менеджера кластера
	Функция СервисыПолучитьСписок(Кластер, АдминистраторКластера)
	
infobase      Режим администрирования информационной базой
	Функция ИБПолучитьОписание(Кластер, База, ПолноеОписание, АдминистраторБазы, АдминистраторКластера)
	Функция ИБПолучитьСписок(Кластер, АдминистраторКластера)
	Процедура ИБСоздать(Кластер, Имя, СоздаватьБД, ТипСУБД, СерверСУБД, ПользовательСУБД, ПарольСУБД, ИмяБД, ЛокальБД, Описание, СмещениеДат, УровеньБезопасности, РазрешитьРегЗадания, РазрешитьВыдачуЛицензий, АдминистраторКластера)
	Процедура ИБИзменить(Кластер, База, ТипСУБД, СерверСУБД, ПользовательСУБД, ПарольСУБД, ИмяБД, Описание, ЗапретДоступаНачало, ЗапретДоступаКонец, ЗапретДоступаСообщение, ЗапретДоступаПараметр, ЗапретДоступаКлюч, ЗапретДоступа, РазрешитьРегЗадания, РазрешитьВыдачуЛицензий, ВнешнееУправлениеПараметр, ВнешнееУправление, ПрофильБезопасности, ПрофильБезопасностиВнеш, АдминистраторБазы, АдминистраторКластера)
	Процедура ИБУдалить(Кластер, База, ОчиститьУдалитьБД, АдминистраторБазы, АдминистраторКластера) 

connection    Режим администрирования соединений
	Функция СоединенияПолучитьОписание(Кластер, Соединение, АдминистраторКластера)
	Функция СоединенияПолучитьСписок(Кластер, Процесс, База, АдминистраторБазы, АдминистраторКластера)
	Процедура СоединенияЗавершить(Кластер, Соединение, АдминистраторБазы, АдминистраторКластера)

session       Режим администрирования сеансов информационных баз
	Функция СеансыПолучитьОписание(Кластер, Сеанс, ПоказыватьЛицензии, АдминистраторКластера)
	Функция СеансыПолучитьСписок(Кластер, База, ПоказыватьЛицензии, АдминистраторКластера)
	Процедура СеансыЗавершить(Кластер, Сеанс, АдминистраторКластера)

lock          Режим администрирования блокировок
	Функция БлокировкиПолучитьСписок(Кластер, База, Соединение, Сеанс, АдминистраторКластера)

rule          Режим управления требованиями назначения
	Функция ТребованияНазначенияПолучитьОписание(Кластер, Сервер, Требование, АдминистраторКластера)
	Функция ТребованияНазначенияПолучитьСписок(Кластер, Сервер, АдминистраторКластера)
	Процедура ТребованияНазначенияПрименить(Кластер, Сервер, ЧастичноеПрименение, АдминистраторКластера)
	Процедура ТребованияНазначенияДобавить(Кластер, Сервер, Позиция, ОбъектТребования, ИмяИБ, ТипТребования, ПриложениеСУточнением, Приоритет, АдминистраторКластера)
	Процедура ТребованияНазначенияИзменить(Кластер, Сервер, Требование, Позиция, ОбъектТребования, ИмяИБ, ТипТребования, ПриложениеСУточнением, Приоритет, АдминистраторКластера)
	Процедура ТребованияНазначенияУдалить(Кластер, Сервер, Требование, АдминистраторКластера)

profile       Режим управления профилями безопасности кластера
	Функция ПрофилиПолучитьСписок(Кластер, АдминистраторКластера)
	Процедура ПрофилиИзменить(Кластер, ПрофильИмя, Описание, РазрешитьИспользование, ПривилегированныйРежим, ФункцииКрипто, РасширениеПрав, РолиРасширенияПрав, ВсеМодули, РазрешенныеМодули, ЗапрещенныеМодули, АдминистраторКластера)
	Процедура ПрофилиУдалить(Кластер, ПрофильИмя, АдминистраторКластера)
	Функция ПрофильКаталогиПолучитьСписок(Кластер, ПрофильИмя, АдминистраторКластера)
	Процедура ПрофильКаталогиУстановитьДоступ(Кластер, ПрофильИмя, Доступ, АдминистраторКластера)
	Процедура ПрофильКаталогиИзменить(Кластер, ПрофильИмя, КаталогУРЛ, КаталогПуть, КаталогОписание, Чтение, Запись, АдминистраторКластера)
	Процедура ПрофильКаталогиУдалить(Кластер, ПрофильИмя, КаталогУРЛ, АдминистраторКластера)
	Функция ПрофильCOMПолучитьСписок(Кластер, ПрофильИмя, АдминистраторКластера)
	Процедура ПрофильCOMУстановитьДоступ(Кластер, ПрофильИмя, Доступ, АдминистраторКластера)
	Процедура ПрофильCOMИзменить(Кластер, ПрофильИмя, КлассИмя, КлассФайл, КлассУИД, КлассХост, КлассОписание, АдминистраторКластера)
	Процедура ПрофильCOMУдалить(Кластер, ПрофильИмя, КлассИмя, АдминистраторКластера)
	Функция ПрофильВКПолучитьСписок(Кластер, ПрофильИмя, АдминистраторКластера)
	Процедура ПрофильВКУстановитьДоступ(Кластер, ПрофильИмя, Доступ, АдминистраторКластера)
	Процедура ПрофильВКИзменить(Кластер, ПрофильИмя, ВКИмя, ВККонтрольнаяСумма, ВКОписание, АдминистраторКластера)
	Процедура ПрофильВКУдалить(Кластер, ПрофильИмя, ВКИмя, АдминистраторКластера)
	Функция ПрофильМодулиПолучитьСписок(Кластер, ПрофильИмя, АдминистраторКластера)
	Процедура ПрофильМодулиУстановитьДоступ(Кластер, ПрофильИмя, Доступ, АдминистраторКластера)
	Процедура ПрофильМодулиИзменить(Кластер, ПрофильИмя, МодульИмя, МодульКонтрольнаяСумма, МодульОписание, АдминистраторКластера)
	Процедура ПрофильМодулиУдалить(Кластер, ПрофильИмя, МодульИмя, АдминистраторКластера)
	Функция ПрофильПриложенияПолучитьСписок(Кластер, ПрофильИмя, АдминистраторКластера)
	Процедура ПрофильПриложенияУстановитьДоступ(Кластер, ПрофильИмя, Доступ, АдминистраторКластера)
	Процедура ПрофильПриложенияИзменить(Кластер, ПрофильИмя, ПриложениеИмя, ПриложениеШаблонЗапуска, ПриложениеОписание, АдминистраторКластера)
	Процедура ПрофильПриложенияУдалить(Кластер, ПрофильИмя, ПриложениеИмя, АдминистраторКластера)
	Функция ПрофильИнтернетПолучитьСписок(Кластер, ПрофильИмя, АдминистраторКластера)
	Процедура ПрофильИнтернетУстановитьДоступ(Кластер, ПрофильИмя, Доступ, АдминистраторКластера)
	Процедура ПрофильИнтернетИзменить(Кластер, ПрофильИмя, РесурсИмя, РесурсПротокол, РесурсУРЛ, РесурсПорт, РесурсОписание, АдминистраторКластера)
	Процедура ПрофильИнтернетУдалить(Кластер, ПрофильИмя, РесурсИмя, АдминистраторКластера)
